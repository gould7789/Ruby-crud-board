<%# d-flex: 유연한 박스가 됨. 이게 없으면 제목과 버튼이 위아래로 쌓임. 가로로 나란히 배치할 준비 %>
<%# justify-content-between: 양쪽 끝으로 배치. <h1>(왼쪽)과 버튼(오른쪽)을 화면 양 끝으로 배치 %>
<%# align-items-center: 위아래 가운데로 정렬. 글씨 크기가 달라도 수직 중앙을 예쁘게 맞춰줌 %>
<%# mb-3: 밑에 여백(Margin Bottom)을 3만큼 줌. 표랑 너무 딱 붙지 않게 띄워 줌 %>
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>자유 게시판</h1>
  <%# btn btn-primary: 파란색 대표 버튼. btn(버튼 모양)과 btn-primary(부트스트랩의 대표색을 입힘) %>
  <%= link_to  "새 글 쓰기", new_post_path, class: "btn btn-primary" %>
</div>

<%# alert: 알림 상자 모양(패딩, 둥근 모서리 등) %>
<%# alert-success: 성공(Success) 색인 초록색으로 채움 %>
<%# alert-danger -> 빨간색, alert-warning -> 노란색 %>
<% if notice %>
  <div class="alert alert-success"><%= notice %></div>
<% end %>

<%# table-hover: 마우스 커서가 올라간 줄(Row)을 살짝 어둡게 함 %>
<%# table-striped: 줄마다 색을 다르게(흰색-회색-흰색) 칠해서 읽기 편하게 함 %>
<table class="table table-hover table-striped">
  <%# table-dark: 머리글(head)을 검은색 배경으로 칠함 %>
  <thead class="table-dark">
    <tr>
      <%# scope="col": 시각장애인용 스크린 리더에게 세로줄의 제목이라고 알려주는 접근성 태그 %>
      <%# style="width": 10%: 번호 칸은 내용이 짧으니, 가로 폭을 10%만 차지하게 함 %>
      <th scope="col" style="width: 10%;">번호</th>
      <th scope="col">제목</th>
      <th scope="col" style="width: 20%;">관리</th>
    </tr>
  </thead>
  <tbody>
    <%# |post,index| -> 게시글(post)과 인덱스(index)를 같이 꺼냅니다 %>
    <% @posts.each_with_index do |post, index| %>
      <tr>
        <%# 게시글 번호 %>
        <th><%= index + 1 %></th>

        <td>
          <%# text-decoration-none: 링크(<a>)는 기본적으로 파란색 밑줄이 있는데, 그것을 지움 %>
          <%# text-dark: 글씨를 검은색으로 변경 %>
          <%# fw-bold: 글씨 두께(Font Weight)를 굵게 변경 %>
          <%= link_to  post.title, post, class: "text-decoration-none text-dark fw-bold" %>
        </td>
        <td>
          <%# d-flex: 버튼들을 가로로 나란히 둠 %>
          <%# gap-2: 버튼 사이사이를 살짝(Gap) 띄워 줌 %>
          <div class="d-flex gap-2">
            <%# btn-sm: 버튼 크기를 작게(Small)함. 표 안에는 공간이 좁으니 작은 버튼이 어울림 %>
            <%# btn-outline-warning: 노란색 테두리만 있는 버튼 %>
            <%= link_to  "수정", edit_post_path(post), class: "btn btn-sm btn-outline-warning" %>

            <%# btn-outline-danger: 빨간색 테두리만 있는 버튼 %>
            <%= link_to post, 
            data: { turbo_method: :delete, turbo_confirm: "정말 삭제하시겠습니까?" }, 
            class: "btn btn-sm btn-outline-danger" do %>
            삭제  
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
